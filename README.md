## ⚙️ Установка и запуск

Для развёртывания проекта на локальной машине необходимо иметь установленные **Docker** и **Composer**.

1.  **Клонируйте репозиторий**
    ```bash
    git clone https://github.com/ZeroKJ123/tz-only
    cd tz-only
    ```

2.  **Создайте файл окружения**
    Скопируйте конфигурационный файл `.env.example` в `.env`. В нём хранятся все настройки приложения.
    ```bash
    cp .env.example .env
    ```

3.  **Установите зависимости PHP**
    ```bash
    composer install
    ```

4.  **Соберите и запустите Docker-контейнеры**
    Эта команда запустит веб-сервер, базу данных и всё необходимое в фоновом режиме.
    ```bash
    ./vendor/bin/sail up -d --build
    ```

5.  **Сгенерируйте ключ приложения**
    Это обязательный шаг для любого Laravel-приложения.
    ```bash
    ./vendor/bin/sail artisan key:generate
    ```

6.  **Запустите миграции и сидеры**
    Команда создаст структуру базы данных и заполнит её тестовыми данными (пользователи, автомобили, должности и т.д.).
    ```bash
    ./vendor/bin/sail artisan migrate:fresh --seed
    ```

🎉 **Готово!** Приложение запущено и доступно по адресу `http://localhost`.

---

## 🔑 Получение токенов для аутентификации

Для взаимодействия с API необходим Bearer-токен. Тестовые данные включают двух пользователей: "Менеджер" и "Директор".

### 1. Токен Менеджера

Запустите `tinker` и выполните следующие команды, чтобы сгенерировать токен:

```bash
./vendor/bin/sail artisan tinker
echo App\Models\User::find(1)->createToken('api-token')->plainTextToken;
```
Скопируйте токен.

### 2. Токен Директора

Запустите `tinker` и выполните следующие команды, чтобы сгенерировать токен:

```bash
./vendor/bin/sail artisan tinker
echo App\Models\User::find(2)->createToken('api-token')->plainTextToken;
```
Скопируйте токен.

---

## 📖 Примеры использования API

### Эндпоинт

-   **URL:** `/api/cars/available`
-   **Метод:** `GET`
-   **Заголовки:**
    -   `Accept: application/json`
    -   `Authorization: Bearer <YOUR_API_TOKEN>`

### Пример 1: Поиск для Менеджера (с проверкой занятости)

Запрос находит доступные автомобили для менеджера на время, которое **пересекается** с существующей бронью. "Kia Rio" не будет в списке.

```bash
curl --location --request GET 'localhost/api/cars/available?start_time=2026-01-16T11:00:00&end_time=2026-01-16T13:00:00' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <ВАШ_ТОКЕН_МЕНЕДЖЕРА>'
```

### Пример 2: Поиск для Директора (с фильтрацией по модели)

Запрос находит доступные автомобили для директора, отфильтрованные по модели "Toyota".

```bash
curl --location --request GET 'http://localhost/api/cars/available?start_time=2026-10-10T10:00:00&end_time=2026-10-10T12:00:00&model=Toyota' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <ВАШ_ТОКЕН_ДИРЕКТОРА>'
```
